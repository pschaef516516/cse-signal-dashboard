---
phase: 03-dashboard-polish-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/SignalModal.jsx
  - src/components/ui/SignalDetail.jsx
  - src/App.jsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "Clicking any signal (from any chart, any tab) opens a centered modal — not the right-side drawer"
    - "Modal header shows '3 of 12 signals' counter with enabled Prev/Next arrow buttons"
    - "Modal closes when user clicks the backdrop, clicks the X button, or presses Escape"
    - "SignalDrawer is no longer rendered in App.jsx"
    - "SignalDetail renders correctly inside the modal body with scrollable content"
  artifacts:
    - path: "src/components/ui/SignalModal.jsx"
      provides: "Centered modal wrapping SignalDetail with Prev/Next navigation"
      exports: ["default SignalModal"]
    - path: "src/components/ui/SignalDetail.jsx"
      provides: "Detail view — back button hidden when onBack is null"
    - path: "src/App.jsx"
      provides: "modalOpen/modalSignals/modalIndex state; openModal/closeModal helpers; SignalModal rendered as last child"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/ui/SignalModal.jsx"
      via: "modalOpen, modalSignals, modalIndex, openModal, closeModal props"
    - from: "src/components/ui/SignalModal.jsx"
      to: "src/components/ui/SignalDetail.jsx"
      via: "<SignalDetail signal={signals[currentIndex]} onBack={null} />"
    - from: "App.jsx chart click handlers"
      to: "openModal(filteredSignals, 0)"
      via: "handleCommunityClick, handleWeekClick, handleSeverityClick"
---

<objective>
Replace the right-side SignalDrawer with a centered SignalModal. Any click that currently opens the drawer must instead open the modal with the same filtered signal list and full Prev/Next navigation.

Purpose: CSMs can triage signals faster in a modal — no back-navigation step, and they can arrow through a list without closing and reopening.
Output: SignalModal.jsx (new), SignalDetail.jsx (minor guard), App.jsx (state swap + wire).
</objective>

<execution_context>
@/Users/pschaef516/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pschaef516/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-polish-ux/03-CONTEXT.md
@.planning/phases/03-dashboard-polish-ux/03-RESEARCH.md

Source files being changed:
- src/App.jsx — main app state and chart handlers
- src/components/ui/SignalDrawer.jsx — being retired (do NOT delete the file yet — just stop importing/rendering it)
- src/components/ui/SignalDetail.jsx — needs onBack guard
- src/components/ui/SignalCard.jsx — used inside the modal via SignalCardList (no changes needed here)

<interfaces>
<!-- Existing state in App.jsx that will be REPLACED -->
// BEFORE (drawer state — remove all four):
const [drawerOpen, setDrawerOpen] = useState(false)
const [drawerTitle, setDrawerTitle] = useState('')
const [drawerSignals, setDrawerSignals] = useState([])
const [selectedSignal, setSelectedSignal] = useState(null)

// AFTER (modal state — add these three):
const [modalOpen, setModalOpen] = useState(false)
const [modalSignals, setModalSignals] = useState([])
const [modalIndex, setModalIndex] = useState(0)

<!-- Existing chart handlers in App.jsx that call openDrawer — all must call openModal instead -->
// handleCommunityClick(sourceName) — filters tabSignals by source, opens drawer → opens modal
// handleWeekClick(weekLabel) — filters tabSignals by ISO week, opens drawer → opens modal
// handleSeverityClick(level) — filters tabSignals by severity, opens drawer → opens modal
// openSignalDetail(signal) — single signal from Browse tab, opens drawer → opens modal

<!-- BrowseTab.jsx currently receives: -->
export default function BrowseTab({ onSignalClick })
// onSignalClick(signal) is called when a row is clicked
// In Phase 03 this will become onSignalClick(signal, allSignals) — see Task 1 note

<!-- SignalDetail.jsx current signature: -->
export default function SignalDetail({ signal, onBack })
// onBack is currently always a function — needs guard so it hides the button when null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SignalModal.jsx</name>
  <files>src/components/ui/SignalModal.jsx</files>
  <action>
Create a new file at src/components/ui/SignalModal.jsx. This component replaces SignalDrawer.

Props: { open, signals, currentIndex, onClose, onPrev, onNext }

Structure:
1. If !open or signals is empty, return null.
2. Render a React Fragment containing two divs:
   a. BACKDROP div — position: fixed, inset: 0, background: rgba(21,24,29,0.5), zIndex: 1000. onClick calls onClose.
   b. MODAL CARD div — position: fixed, top: 50%, left: 50%, transform: translate(-50%,-50%), width: 700, maxWidth: calc(100vw - 32px), maxHeight: calc(100vh - 64px), background: #FFFFFF, border: 1px solid #E1E6F2, borderRadius: 16, zIndex: 1001, display: flex, flexDirection: column, overflow: hidden. onClick calls e.stopPropagation() to prevent backdrop-close from firing.

Modal card contains two sections:
  HEADER div — display: flex, alignItems: center, justifyContent: space-between, padding: 16px 20px, borderBottom: 1px solid #E1E6F2, flexShrink: 0.
    Left side: a div with display:flex, alignItems:center, gap:12 containing:
      - Prev button (←): onClick calls onPrev. Disabled when currentIndex === 0. When disabled: cursor not-allowed, color #E1E6F2. When enabled: color #15181D. Style: background none, border 1px solid #E1E6F2, borderRadius 6, padding 4px 10px, fontSize 16. aria-label="Previous signal".
      - Counter span: "{currentIndex + 1} of {signals.length} signals". fontSize 13, color #6B7487, fontWeight 500.
      - Next button (→): onClick calls onNext. Disabled when currentIndex === signals.length - 1. Same disabled/enabled style logic as Prev. aria-label="Next signal".
    Right side: Close button (×). onClick calls onClose. Style: background none, border none, fontSize 20, color #6B7487, cursor pointer, padding 4, lineHeight 1. aria-label="Close modal".

  BODY div — padding: 20, overflowY: auto, flex: 1.
    Renders: <SignalDetail signal={signals[currentIndex]} onBack={null} />

3. Add useEffect for keyboard handling. Dependency array: [open, currentIndex, signals.length, onClose, onPrev, onNext].
   - If !open, return early (no listener attached).
   - Add document.addEventListener('keydown', handler).
   - Handler: Escape → onClose. ArrowLeft when currentIndex > 0 → onPrev. ArrowRight when currentIndex < signals.length - 1 → onNext.
   - Return cleanup: document.removeEventListener('keydown', handler).

4. Import useEffect from 'react'. Import SignalDetail from './SignalDetail'.
5. No className attributes anywhere — inline styles only.
  </action>
  <verify>
File exists at src/components/ui/SignalModal.jsx. Opens in editor without syntax errors (run: node --input-type=module --eval "import('./src/components/ui/SignalModal.jsx')" will fail in Node but Vite will catch parse errors on next dev start).
  </verify>
  <done>SignalModal.jsx exists, exports a default function, contains backdrop + modal card + header with Prev/counter/Next/X + scrollable body rendering SignalDetail.</done>
</task>

<task type="auto">
  <name>Task 2: Guard onBack in SignalDetail.jsx</name>
  <files>src/components/ui/SignalDetail.jsx</files>
  <action>
In SignalDetail.jsx, the "← Back to signals" button is currently rendered unconditionally. When rendered inside SignalModal, onBack will be null — clicking it would throw.

Find the back button block (lines 61–77 approximately):
```jsx
<button
  onClick={onBack}
  style={{ ... }}
>
  ← Back to signals
</button>
```

Wrap the entire button in a conditional so it only renders when onBack is not null:
```jsx
{onBack && (
  <button
    onClick={onBack}
    style={{ ... }}
  >
    ← Back to signals
  </button>
)}
```

No other changes to SignalDetail.jsx.
  </action>
  <verify>Visually confirm: when SignalDetail is rendered with onBack={null}, no back button appears. When rendered with onBack={someFunction}, the button appears.</verify>
  <done>Back button is conditionally rendered. The rest of SignalDetail is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Swap drawer for modal in App.jsx</name>
  <files>src/App.jsx</files>
  <action>
Make the following changes to App.jsx in order. Read the full file before editing.

STEP A — Update imports:
- Remove: import SignalDrawer from './components/ui/SignalDrawer'
- Remove: import SignalDetail from './components/ui/SignalDetail' (it is now used only inside SignalModal)
- Remove: import { SignalCardList } from './components/ui/SignalCard' (no longer used in App.jsx)
- Add: import SignalModal from './components/ui/SignalModal'

STEP B — Replace drawer state with modal state.
Remove these four useState declarations:
  const [drawerOpen, setDrawerOpen] = useState(false)
  const [drawerTitle, setDrawerTitle] = useState('')
  const [drawerSignals, setDrawerSignals] = useState([])
  const [selectedSignal, setSelectedSignal] = useState(null)

Add these three useState declarations in their place:
  const [modalOpen, setModalOpen] = useState(false)
  const [modalSignals, setModalSignals] = useState([])
  const [modalIndex, setModalIndex] = useState(0)

STEP C — Replace drawer helpers with modal helpers.
Remove openDrawer, closeDrawer, and openSignalDetail functions entirely.
Add these two functions:
  function openModal(signals, startIndex = 0) {
    setModalSignals(signals)
    setModalIndex(startIndex)
    setModalOpen(true)
  }

  function closeModal() {
    setModalOpen(false)
  }

STEP D — Update chart click handlers.
All three handlers currently call openDrawer. Change them to call openModal:
  handleCommunityClick: change openDrawer(..., filtered) to openModal(filtered, 0)
  handleWeekClick: change openDrawer(..., filtered) to openModal(filtered, 0)
  handleSeverityClick: change openDrawer(..., filtered) to openModal(filtered, 0)

Remove the tabLabel variable if it is now only used inside the old openDrawer title strings. (Check: if it still appears elsewhere, keep it. If not, remove it.)

STEP E — Update BrowseTab's onSignalClick prop.
The old prop passed: onSignalClick={openSignalDetail} where openSignalDetail only took one signal.
Change to pass an inline handler that accepts both signal and allSignals:
  <BrowseTab onSignalClick={(signal, allSignals) => {
    const list = allSignals && allSignals.length > 0 ? allSignals : [signal]
    const idx = list.indexOf(signal)
    openModal(list, idx >= 0 ? idx : 0)
  }} />

STEP F — Replace SignalDrawer JSX with SignalModal JSX.
Find and remove the entire SignalDrawer block at the bottom of the return statement:
  <SignalDrawer open={drawerOpen} ...>
    {selectedSignal ? (
      <SignalDetail ... />
    ) : (
      <SignalCardList ... />
    )}
  </SignalDrawer>

Replace it with the SignalModal, placed as the LAST child inside the outermost <div style={{ minHeight: '100vh', background: '#FAFBFF' }}> — after all the content panels but inside the root div:
  <SignalModal
    open={modalOpen}
    signals={modalSignals}
    currentIndex={modalIndex}
    onClose={closeModal}
    onPrev={() => setModalIndex((i) => Math.max(0, i - 1))}
    onNext={() => setModalIndex((i) => Math.min(modalSignals.length - 1, i + 1))}
  />

IMPORTANT: Do not change any other logic — stat card derivations, chart rendering, tab switching, FilterPills, and BrowseTab rendering all remain as-is.
  </action>
  <verify>Run: cd "/Users/pschaef516/CSE- Dashboard" && npm run build 2>&1 | tail -20. Expect "built in" success message. No "Cannot find module" or "is not defined" errors.</verify>
  <done>App.jsx builds without errors. SignalDrawer is no longer imported. SignalModal is imported and rendered as last child of root div. Modal state uses modalOpen/modalSignals/modalIndex. All three chart handlers call openModal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → browser | Signal data arrives from external API and is rendered in modal body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | SignalModal keyboard handler | accept | Handler only calls onClose/onPrev/onNext — no user input is processed; low-value target |
| T-03-01-02 | Information Disclosure | signal.text / signal.key_quote rendered in modal | accept | Internal dashboard, no PII beyond what already renders in SignalDetail |
| T-03-01-03 | Denial of Service | modalSignals array very large | accept | All data already in memory from initial fetch; rendering is client-side only |
</threat_model>

<verification>
1. Start dev server: npm run dev in the project directory.
2. Open http://localhost:5173 in a browser.
3. Click any bar on the Signals by Community chart — a centered modal should appear (not a right-side drawer).
4. Confirm modal header shows "1 of N signals" with Prev disabled (gray) and Next enabled (if N > 1).
5. Click Next arrow — counter advances to "2 of N". Click Prev — returns to "1 of N".
6. Press Escape — modal closes.
7. Click a bar again — modal opens. Click the backdrop (outside the card) — modal closes.
8. Click the X button — modal closes.
9. Switch to Browse tab — click any signal row — modal opens showing "1 of N signals" where N is the count of all signals loaded for that date, not 1.
10. Confirm the right-side drawer never appears.
</verification>

<success_criteria>
- SignalModal.jsx exists and exports a default function component
- SignalDetail.jsx back button is wrapped in {onBack && (...)}
- App.jsx has no reference to SignalDrawer, openDrawer, closeDrawer, or selectedSignal
- npm run build completes without errors
- Modal opens from all three chart click handlers (Community, Week, Severity)
- Modal opens from Browse tab row clicks with full list navigation
- Escape, backdrop click, and X button all close the modal
</success_criteria>

<output>
After completion, create .planning/phases/03-dashboard-polish-ux/03-01-SUMMARY.md with:
- What was built (SignalModal, guard in SignalDetail, App.jsx state swap)
- Key implementation details (state shape: modalOpen/modalSignals/modalIndex)
- Anything unexpected encountered
- Confirmation that all success criteria passed
</output>
