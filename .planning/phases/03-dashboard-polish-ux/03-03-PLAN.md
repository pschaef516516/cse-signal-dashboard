---
phase: 03-dashboard-polish-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/ui/FilterPills.jsx
  - src/App.jsx
autonomous: true
requirements:
  - D-10
  - D-11

must_haves:
  truths:
    - "Churn tab header row shows: Today | Yesterday | Week ↓ | Month ↓ | All filter options"
    - "E&U tab header row shows the same five options"
    - "Today and Yesterday are single-click buttons that instantly filter to that calendar day"
    - "Week button opens a dropdown listing the last 12 ISO weeks by human-readable range"
    - "Month button opens a dropdown listing the last 12 calendar months by name"
    - "All shows all-time data (existing null behavior)"
    - "'Last Week' means the previous full Mon–Sun calendar week, not a rolling 7-day window"
    - "Active filter pill is visually filled/blue; inactive pills are outlined/gray"
    - "Per-tab state: selecting a filter on Churn does not change E&U filter and vice versa"
  artifacts:
    - path: "src/components/ui/FilterPills.jsx"
      provides: "Calendar-anchored time filter replacing 7d/30d/90d pills"
      exports: ["default FilterPills"]
    - path: "src/App.jsx"
      provides: "churnTimeFilter/enrollmentTimeFilter state objects; filterByTimeFilter helper applied to tabSignals"
  key_links:
    - from: "App.jsx churnTimeFilter/enrollmentTimeFilter state"
      to: "FilterPills component"
      via: "value and onChange props"
    - from: "App.jsx"
      to: "dateRanges.js getWeekRange / getMonthRange / filterByRange"
      via: "filterByTimeFilter helper function"
    - from: "App.jsx tabSignals derivation"
      to: "filterByTimeFilter(tabSignalsByType, activeFilter)"
      via: "replaces filterByDateRange(tabSignalsByType, activeFilter)"
---

<objective>
Replace the 7d/30d/90d rolling filter pills on the Churn and E&U tabs with calendar-anchored options: Today, Yesterday, Week (dropdown), Month (dropdown), All.

Purpose: CSMs review signals week-by-week and month-by-month. "Last 30 days" crosses month boundaries and is confusing for reporting. Calendar-anchored filters match how CSMs actually talk about their work.
Output: FilterPills.jsx (rewritten), App.jsx (state shape updated, filter derivation updated).
</objective>

<execution_context>
@/Users/pschaef516/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pschaef516/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-polish-ux/03-CONTEXT.md
@.planning/phases/03-dashboard-polish-ux/03-RESEARCH.md
@.planning/phases/03-dashboard-polish-ux/03-01-SUMMARY.md

Source files being changed:
- src/components/ui/FilterPills.jsx — rewrite (was simple 4-button pill row; becomes calendar-anchored filter bar)
- src/App.jsx — update churnFilter/enrollmentFilter state shape; replace filterByDateRange call with new helper

dateRanges.js was created in Plan 03-02. This plan depends on it being present.
If 03-02 has not run yet, dateRanges.js may not exist — in that case, create a minimal version of it here containing only: getTodayRange, getYesterdayRange, getWeekRange, getMonthRange, getRecentWeeks, getRecentMonths, formatWeekRangeLabel, filterByRange. (Identical to what 03-02 would create — safe to create first, 03-02 will overwrite with same content.)

<interfaces>
<!-- Current App.jsx state being replaced -->
const [churnFilter, setChurnFilter] = useState(null)       // null | 7 | 30 | 90 (number of days)
const [enrollmentFilter, setEnrollmentFilter] = useState(null)

// REPLACED BY:
const [churnTimeFilter, setChurnTimeFilter] = useState({ mode: 'all', weekValue: null, monthValue: null })
const [enrollmentTimeFilter, setEnrollmentTimeFilter] = useState({ mode: 'all', weekValue: null, monthValue: null })

<!-- Current App.jsx filter derivation being replaced -->
const activeFilter = isChurn ? churnFilter : enrollmentFilter
const tabSignals = filterByDateRange(tabSignalsByType, activeFilter)
const filteredPosts = filterByDateRange(posts, activeFilter, 'captured_date')
const filteredAllSignals = filterByDateRange(signals, activeFilter)

// REPLACED BY a new filterByTimeFilter helper function (defined in App.jsx).

<!-- FilterPills new props -->
// value: { mode: 'today' | 'yesterday' | 'week' | 'month' | 'all', weekValue: null | 'YYYY-WXX', monthValue: null | 'YYYY-MM' }
// onChange: function(newValue)

<!-- dateRanges.js exports needed here -->
import { getTodayRange, getYesterdayRange, getWeekRange, getMonthRange, getRecentWeeks, getRecentMonths, formatWeekRangeLabel, formatMonthLabel, filterByRange } from '../utils/dateRanges'
// (import path relative to App.jsx is './utils/dateRanges')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FilterPills.jsx as calendar-anchored time filter</name>
  <files>src/components/ui/FilterPills.jsx</files>
  <action>
Completely rewrite FilterPills.jsx. The old file had a simple OPTIONS array and four pill buttons. The new version has five options where Week and Month open dropdown pickers.

Props: { value, onChange }
- value: object with shape { mode: string, weekValue: string|null, monthValue: string|null }
- onChange: function(newValueObject)

Imports needed:
- import { useState, useEffect, useRef } from 'react'
- import { getRecentWeeks, getRecentMonths, formatWeekRangeLabel, formatMonthLabel } from '../../utils/dateRanges'

State:
- const [weekOpen, setWeekOpen] = useState(false)
- const [monthOpen, setMonthOpen] = useState(false)
- const weekRef = useRef(null)
- const monthRef = useRef(null)

Add two useEffect hooks for click-outside detection — one for weekOpen and one for monthOpen.
Each follows the same pattern as BrowseFilterPill (from Plan 03-02):
  if (!weekOpen) return
  function handleMouseDown(e) { if (weekRef.current && !weekRef.current.contains(e.target)) setWeekOpen(false) }
  document.addEventListener('mousedown', handleMouseDown)
  return () => document.removeEventListener('mousedown', handleMouseDown)
(Same pattern for monthOpen/monthRef.)

Precompute:
- const weekOptions = getRecentWeeks(12)
- const monthOptions = getRecentMonths(12)

BUTTON HELPERS — define inline style functions:
```javascript
function pillStyle(active) {
  return {
    padding: '8px 12px',
    fontSize: 12,
    fontWeight: 600,
    borderRadius: 20,
    border: active ? '1px solid #0057FF' : '1px solid #E1E6F2',
    background: active ? '#0057FF' : '#FFFFFF',
    color: active ? '#FFFFFF' : '#6B7487',
    cursor: 'pointer',
    minHeight: 36,
  }
}
```

RETURN JSX — a div with display: flex, gap: 8, alignItems: center, paddingBottom: 8:

1. TODAY button: onClick={() => onChange({ mode: 'today', weekValue: null, monthValue: null }). active = value.mode === 'today'. Style: pillStyle(active). Text: "Today".

2. YESTERDAY button: same pattern, mode: 'yesterday'. Text: "Yesterday".

3. WEEK button — wrapped in a position:relative div with ref={weekRef}:
   - Pill button: onClick toggles weekOpen. active = value.mode === 'week'. Text: value.mode === 'week' && value.weekValue ? `${formatWeekRangeLabel(value.weekValue)} ↑` : 'Week ↓'
   - If weekOpen, render dropdown div below (same dropdown style as BrowseFilterPill: position absolute, top calc(100%+4px), left 0, background #FFF, border 1px solid #E1E6F2, borderRadius 8, boxShadow 0 4px 16px rgba(0,0,0,0.10), zIndex 100, minWidth 180, overflow hidden):
     - Map weekOptions to buttons. Each onClick: onChange({ mode: 'week', weekValue: w, monthValue: null }); setWeekOpen(false). Text: formatWeekRangeLabel(w). Active highlight if value.weekValue === w.

4. MONTH button — same pattern, ref={monthRef}, monthOpen state:
   - Text: value.mode === 'month' && value.monthValue ? `${formatMonthLabel(value.monthValue)} ↑` : 'Month ↓'
   - Dropdown: map monthOptions, each onClick: onChange({ mode: 'month', weekValue: null, monthValue: m }); setMonthOpen(false). Text: formatMonthLabel(m).

5. ALL button: onClick={() => onChange({ mode: 'all', weekValue: null, monthValue: null }). active = value.mode === 'all'. Text: "All".

No className anywhere. Export default FilterPills.
  </action>
  <verify>npm run build passes. No syntax errors.</verify>
  <done>FilterPills.jsx exports a default function. Renders Today, Yesterday, Week ↓, Month ↓, All pills. Week and Month have working dropdowns via click-outside detection. Active pill is blue/filled.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.jsx filter state and derivation logic</name>
  <files>src/App.jsx</files>
  <action>
Read App.jsx before editing (it was already modified in Plan 03-01). Make the following targeted changes.

STEP A — Add dateRanges import.
Add this import near the top of App.jsx, after the existing utils imports:
  import { getTodayRange, getYesterdayRange, getWeekRange, getMonthRange, filterByRange } from './utils/dateRanges'

STEP B — Replace filter state declarations.
Find:
  const [churnFilter, setChurnFilter] = useState(null)
  const [enrollmentFilter, setEnrollmentFilter] = useState(null)

Replace with:
  const [churnTimeFilter, setChurnTimeFilter] = useState({ mode: 'all', weekValue: null, monthValue: null })
  const [enrollmentTimeFilter, setEnrollmentTimeFilter] = useState({ mode: 'all', weekValue: null, monthValue: null })

STEP C — Add filterByTimeFilter helper function.
Add this function inside the App component, before the return statement. It converts the filter value object into a filtered array:

```javascript
function filterByTimeFilter(rows, filter, dateField = 'created_at') {
  if (!filter || filter.mode === 'all') return rows
  if (filter.mode === 'today') {
    return filterByRange(rows, getTodayRange(), dateField)
  }
  if (filter.mode === 'yesterday') {
    return filterByRange(rows, getYesterdayRange(), dateField)
  }
  if (filter.mode === 'week') {
    const weekKey = filter.weekValue || null
    if (!weekKey) return rows  // no week selected yet — show all
    return filterByRange(rows, getWeekRange(weekKey), dateField)
  }
  if (filter.mode === 'month') {
    const monthKey = filter.monthValue || null
    if (!monthKey) return rows  // no month selected yet — show all
    return filterByRange(rows, getMonthRange(monthKey), dateField)
  }
  return rows
}
```

STEP D — Replace the filter derivation block.
Find the block:
  const activeFilter = isChurn ? churnFilter : enrollmentFilter
  const tabSignals = filterByDateRange(tabSignalsByType, activeFilter)
  const filteredPosts = filterByDateRange(posts, activeFilter, 'captured_date')
  const filteredAllSignals = filterByDateRange(signals, activeFilter)

Replace with:
  const activeTimeFilter = isChurn ? churnTimeFilter : enrollmentTimeFilter
  const tabSignals = filterByTimeFilter(tabSignalsByType, activeTimeFilter)
  const filteredPosts = filterByTimeFilter(posts, activeTimeFilter, 'captured_date')
  const filteredAllSignals = filterByTimeFilter(signals, activeTimeFilter)

You may also remove the import of filterByDateRange from './utils/aggregate' if it is no longer used anywhere else in App.jsx. Check first — if filterByDateRange appears anywhere else, keep the import.

STEP E — Update FilterPills rendering in JSX.
Find the FilterPills usage (it renders in the header row when !isBrowse):
```jsx
<FilterPills
  value={isChurn ? churnFilter : enrollmentFilter}
  onChange={(days) => {
    if (isChurn) setChurnFilter(days)
    else setEnrollmentFilter(days)
  }}
/>
```

Replace with:
```jsx
<FilterPills
  value={isChurn ? churnTimeFilter : enrollmentTimeFilter}
  onChange={(newFilter) => {
    if (isChurn) setChurnTimeFilter(newFilter)
    else setEnrollmentTimeFilter(newFilter)
  }}
/>
```

No other changes to App.jsx.
  </action>
  <verify>
1. npm run build — must pass with no errors.
2. Open the app, go to Churn tab.
3. Confirm the filter bar shows: Today | Yesterday | Week ↓ | Month ↓ | All (not 7d / 30d / 90d).
4. Click "Today" — stat cards update to show only today's signals. "All" is no longer active (blue).
5. Click "Week ↓" — a dropdown appears with human-readable week ranges.
6. Select a week — the pills row shows the selected week label, stat cards update.
7. Switch to E&U tab — filter resets to its own independent state (does not inherit Churn filter).
8. Click "All" — returns to all-time view.
  </verify>
  <done>
- App.jsx uses churnTimeFilter/enrollmentTimeFilter with { mode, weekValue, monthValue } shape
- filterByTimeFilter helper correctly routes to getTodayRange/getYesterdayRange/getWeekRange/getMonthRange
- FilterPills renders the five calendar-anchored options
- Week and Month dropdowns open and close correctly
- Per-tab filter state is independent
- npm run build passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filter selection → client-side data derivation | Filter state is used only for client-side array filtering, not sent to Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | filterByTimeFilter with user-supplied weekValue/monthValue | accept | Values come from getRecentWeeks/getRecentMonths outputs — hardcoded format strings; no user-typed input reaches Supabase in this plan |
| T-03-03-02 | Denial of Service | filterByRange called on large arrays repeatedly | accept | All data already in memory; .filter() on 10k rows is sub-millisecond |
</threat_model>

<verification>
All items in Task 2 verify block, plus:
- Selecting "Yesterday" on a day with no signals shows 0 signals and appropriate empty state (not a crash)
- Selecting "Week ↓" without choosing a specific week shows all-time (graceful default)
- The SectionHeader subtitle still reads "All-time totals" — consider updating it dynamically if time allows, but it is not a blocker
</verification>

<success_criteria>
- FilterPills.jsx no longer has the 7d/30d/90d OPTIONS array
- FilterPills renders Today, Yesterday, Week ↓, Month ↓, All
- App.jsx state uses churnTimeFilter/enrollmentTimeFilter (not churnFilter/enrollmentFilter)
- tabSignals, filteredPosts, filteredAllSignals all go through filterByTimeFilter
- filterByDateRange is removed from App.jsx if no longer needed
- npm run build passes
- Changing Churn filter does not affect E&U filter
</success_criteria>

<output>
After completion, create .planning/phases/03-dashboard-polish-ux/03-03-SUMMARY.md with:
- What was built
- New filter state shape documented
- Any edge cases encountered (empty week/month, today with no data)
- Confirmation all success criteria passed
</output>
