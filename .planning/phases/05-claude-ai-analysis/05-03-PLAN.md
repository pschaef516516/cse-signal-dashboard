---
phase: 05-claude-ai-analysis
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - D-01
  - D-02
must_haves:
  truths:
    - "App.jsx TABS array contains a 5th entry { id: 'ai', label: 'AI Insights' }"
    - "isAI boolean exists and is derived from activeTab === 'ai'"
    - "FilterPills are hidden when isAI is true"
    - "Main churn/E&U content block is hidden when isAI is true"
    - "AIInsightsTab is rendered when isAI is true, receiving the full signals array"
  artifacts:
    - path: "src/App.jsx"
      provides: "Tab wiring + content block for AI Insights"
      contains: "AIInsightsTab"
  key_links:
    - from: "src/App.jsx"
      to: "src/components/ui/AIInsightsTab.jsx"
      via: "import + isAI conditional render"
      pattern: "import AIInsightsTab"
    - from: "src/App.jsx TABS array"
      to: "tab row UI"
      via: "{ id: 'ai', label: 'AI Insights' }"
      pattern: "id: 'ai'"
---

<objective>
Wire `AIInsightsTab` into `App.jsx`: add the 5th tab to the TABS array, add the `isAI` boolean, add `!isAI` to both visibility guards (FilterPills row + main content block), and render `<AIInsightsTab signals={signals} />` when the tab is active.

Purpose: Makes the AI Insights tab clickable and keeps the existing Churn/E&U/Browse/Pipeline behavior unchanged when other tabs are active.

Output: `src/App.jsx` modified in 5 specific spots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-claude-ai-analysis/05-CONTEXT.md
@.planning/phases/05-claude-ai-analysis/05-PATTERNS.md
@src/App.jsx

<interfaces>
<!-- Component this plan wires up — created by plan 05-02. Do NOT modify it. -->
import AIInsightsTab from './components/ui/AIInsightsTab'
<AIInsightsTab signals={signals} />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire AIInsightsTab into App.jsx (5 edits)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (full file — must understand existing structure before editing)
    - .planning/phases/05-claude-ai-analysis/05-PATTERNS.md (App.jsx section — 6 exact edit locations)
  </read_first>
  <action>
Make exactly 5 small edits to `src/App.jsx`. Do NOT change anything else. Do NOT modify the AIInsightsTab component (that is plan 05-02's territory).

**Edit 1 — Import (after PipelineTab import, around line 12):**
Find:
```javascript
import PipelineTab from './components/ui/PipelineTab'
```
Add immediately after it:
```javascript
import AIInsightsTab from './components/ui/AIInsightsTab'
```

**Edit 2 — TABS array (around lines 23-28):**
Find:
```javascript
const TABS = [
  { id: 'churn', label: 'Churn' },
  { id: 'enrollment', label: 'Enrollment & Upsell' },
  { id: 'browse', label: 'Browse' },
  { id: 'pipeline', label: 'Pipeline' },
]
```
Add the new entry as the LAST item, preserving trailing comma style:
```javascript
const TABS = [
  { id: 'churn', label: 'Churn' },
  { id: 'enrollment', label: 'Enrollment & Upsell' },
  { id: 'browse', label: 'Browse' },
  { id: 'pipeline', label: 'Pipeline' },
  { id: 'ai', label: 'AI Insights' },
]
```

**Edit 3 — Boolean flag (around line 133):**
Find:
```javascript
const isPipeline = activeTab === 'pipeline'
const activeTimeFilter = isChurn ? churnTimeFilter : enrollmentTimeFilter
```
Insert a new `isAI` line BETWEEN them:
```javascript
const isPipeline = activeTab === 'pipeline'
const isAI = activeTab === 'ai'
const activeTimeFilter = isChurn ? churnTimeFilter : enrollmentTimeFilter
```

**Edit 4 — FilterPills guard (around line 256):**
Find:
```javascript
{!isBrowse && !isPipeline && (
  <FilterPills
```
Change to:
```javascript
{!isBrowse && !isPipeline && !isAI && (
  <FilterPills
```

**Edit 5a — Main content guard (around line 272):**
Find:
```javascript
{!isBrowse && !isPipeline && (
  <>
    {/* Key Metrics */}
```
Change to:
```javascript
{!isBrowse && !isPipeline && !isAI && (
  <>
    {/* Key Metrics */}
```

**Edit 5b — Render block (immediately after the `isPipeline` block, around line 447):**
Find:
```javascript
        {isPipeline && (
          <div style={{ marginBottom: 32 }}>
            <PipelineTab signals={signals} posts={posts} />
          </div>
        )}

      </div>
```
Insert a new `isAI` block BEFORE the closing `</div>` (matching the same wrapper style with `marginBottom: 32`):
```javascript
        {isPipeline && (
          <div style={{ marginBottom: 32 }}>
            <PipelineTab signals={signals} posts={posts} />
          </div>
        )}

        {isAI && (
          <div style={{ marginBottom: 32 }}>
            <AIInsightsTab signals={signals} />
          </div>
        )}

      </div>
```

After all edits, run `npm run build` to confirm it compiles. If it fails, re-read the file and fix.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import AIInsightsTab from './components/ui/AIInsightsTab'" src/App.jsx` returns 1
    - `grep -c "{ id: 'ai', label: 'AI Insights' }" src/App.jsx` returns 1
    - `grep -c "const isAI = activeTab === 'ai'" src/App.jsx` returns 1
    - `grep -c "!isBrowse && !isPipeline && !isAI" src/App.jsx` returns 2 (FilterPills guard + main content guard)
    - `grep -c "<AIInsightsTab signals={signals} />" src/App.jsx` returns 1
    - `grep -c "{isAI && (" src/App.jsx` returns 1
    - `grep -c "import PipelineTab" src/App.jsx` returns 1 (existing import preserved)
    - `grep -c "{ id: 'pipeline', label: 'Pipeline' }" src/App.jsx` returns 1 (existing tab preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>App.jsx renders the AI Insights tab as the 5th option, hides FilterPills + main content when active, and mounts AIInsightsTab with the full unfiltered signals array.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Existing tabs (Churn, E&U, Browse, Pipeline) still work — unchanged behavior
- AI Insights tab is clickable; clicking it hides the FilterPills bar and the churn/E&U content
- AIInsightsTab receives the full signals array (not filtered by tab)
</verification>

<success_criteria>
- 5 specific edits made to src/App.jsx, no other changes
- TABS array has exactly 5 entries, with `ai` as the last
- isAI boolean derived from activeTab
- Both `!isBrowse && !isPipeline` guards updated to include `!isAI`
- isAI conditional block renders AIInsightsTab with `signals={signals}` (full array, per D-04)
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-claude-ai-analysis/05-03-SUMMARY.md` covering: edits made, build status, any drift from the 5 specified edit points.
</output>
