---
phase: 04-browse-ux-data-clarity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/PipelineTab.jsx
autonomous: true
requirements: [D-08, D-09]
must_haves:
  truths:
    - "Pipeline tab shows the text 'Updated daily through yesterday' as part of the Posts Ingested BigStat sub-text on every time filter selection"
    - "The note styling matches existing BigStat sub styling: fontSize 12, color #6B7487"
  artifacts:
    - path: "src/components/ui/PipelineTab.jsx"
      provides: "Updated sub prop on Posts Ingested BigStat"
      contains: "Updated daily through yesterday"
  key_links:
    - from: "src/components/ui/PipelineTab.jsx Posts Ingested BigStat"
      to: "BigStat sub paragraph render"
      via: "sub prop"
      pattern: "Updated daily through yesterday"
---

<objective>
Add the pipeline delay note to the Posts Ingested stat card on the Pipeline tab. This is a single-string change to one BigStat invocation.

Purpose: Posts are scraped one day in arrears (the scraping team is in India). The Posts Ingested card showing 0 on Today is correct but confusing without context. A subtext note clarifies the pipeline delay so CSMs don't think the pipeline is broken.

Output: Updated `src/components/ui/PipelineTab.jsx` with the new sub string on the Posts Ingested BigStat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-browse-ux-data-clarity/04-CONTEXT.md
@.planning/phases/04-browse-ux-data-clarity/04-RESEARCH.md
@.planning/phases/04-browse-ux-data-clarity/04-PATTERNS.md
@src/components/ui/PipelineTab.jsx

<interfaces>
<!-- BigStat already supports the sub prop (lines 16-30) -->
function BigStat({ label, value, sub }) {
  return (
    <div ...>
      <p>{label}</p>
      <p>{value}</p>
      {sub && <p style={{ fontSize: 12, color: '#6B7487', margin: 0 }}>{sub}</p>}
    </div>
  )
}

<!-- Current Posts Ingested invocation (around lines 53-57) -->
<BigStat
  label="Posts Ingested"
  value={totalPosts.toLocaleString()}
  sub="All-time community posts scraped"
/>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update Posts Ingested BigStat sub prop in PipelineTab.jsx</name>
  <files>src/components/ui/PipelineTab.jsx</files>
  <read_first>
    - src/components/ui/PipelineTab.jsx (full file — focus on BigStat function around lines 16-30 and the Posts Ingested invocation around lines 53-57)
    - .planning/phases/04-browse-ux-data-clarity/04-CONTEXT.md (D-08, D-09)
  </read_first>
  <behavior>
    - Test 1: Pipeline tab Posts Ingested card displays "Updated daily through yesterday" as part of its sub text on every time filter selection (the sub is unconditional — no ternary)
    - Test 2: Sub text styling stays at fontSize 12, color #6B7487 (handled automatically because it goes through BigStat's existing sub render)
  </behavior>
  <action>
    1. Locate the Posts Ingested BigStat invocation in `src/components/ui/PipelineTab.jsx` (around line 56). Current code:
       ```jsx
       <BigStat
         label="Posts Ingested"
         value={totalPosts.toLocaleString()}
         sub="All-time community posts scraped"
       />
       ```

    2. Replace the `sub` prop value with the combined string per D-08 (preserves existing context AND adds the delay note). Use a middle dot separator to match other multi-part sub strings in the file (if any) or just join with a separator:
       ```jsx
       <BigStat
         label="Posts Ingested"
         value={totalPosts.toLocaleString()}
         sub="All-time community posts scraped · Updated daily through yesterday"
       />
       ```

    3. Make NO other changes. Per D-08 and Pitfall 4: the sub is unconditional — no ternary based on time filter, no conditional rendering. The styling (D-09: fontSize 12, color #6B7487) is already handled by BigStat's existing `sub` paragraph render at line 27.

    Implements D-08, D-09.
  </action>
  <verify>
    <automated>grep -c "Updated daily through yesterday" src/components/ui/PipelineTab.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Updated daily through yesterday" src/components/ui/PipelineTab.jsx` returns exactly 1
    - `grep -n "All-time community posts scraped" src/components/ui/PipelineTab.jsx` returns exactly 1 line, and that line is on the same `sub=` prop assignment as the new note
    - No new ternary, no `if`/`?:` logic added around the sub prop (the string is a constant)
    - `npm run build` exits 0 (run as separate sanity check after the grep)
    - Visual: Open Pipeline tab — Posts Ingested card shows "All-time community posts scraped · Updated daily through yesterday" below the value, in muted small text
  </acceptance_criteria>
  <done>
    Posts Ingested BigStat sub prop includes the delay note. No other changes to the file. Build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — single string change to a sub prop. No data flow, no user input, no API surface affected.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | N/A | src/components/ui/PipelineTab.jsx | accept | No trust boundary crossed. Static string update. |
</threat_model>

<verification>
- `npm run build` succeeds
- Dev server: Pipeline tab Posts Ingested card shows the new sub text
- Sub text remains the same string regardless of which time filter is selected (because Pipeline tab does not consume time filter state)
</verification>

<success_criteria>
All `must_haves.truths` are observable. `npm run build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/04-browse-ux-data-clarity/04-03-SUMMARY.md` documenting:
- Exact line changed in PipelineTab.jsx
- Confirmation that no conditional logic was added (sub is unconditional per D-08)
</output>
