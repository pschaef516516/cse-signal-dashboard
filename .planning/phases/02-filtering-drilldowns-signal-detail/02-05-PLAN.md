---
phase: 02
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "User can switch the time period and see all stat cards + charts update on the active tab"
    - "User can click a community/week/severity bar and see the drawer slide in"
    - "User can click a signal card and see the detail view (key_quote, summary, suggested_action)"
    - "User can click the backdrop to close the drawer"
    - "User sees the E&U tab enriched with enrollment/upsell stat cards and split charts"
    - "User sees no Top Orgs table on either tab"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: end-to-end interaction validation against running dev server. Confirms that all D-XX decisions implemented across Plans 01–04 produce the user-visible behavior the user agreed to in CONTEXT.md.

Purpose: Plans 01–04 cover unit-testable code; this plan covers the visual/interactive behavior that grep cannot verify. The dashboard is interactive — actual click flows need a human eye.

Output: User confirmation (or a list of specific bugs to fix in a follow-up gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-filtering-drilldowns-signal-detail/02-CONTEXT.md
@.planning/phases/02-filtering-drilldowns-signal-detail/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end visual + interaction verification</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/02-filtering-drilldowns-signal-detail/02-CONTEXT.md (locked decisions D-01 through D-14)
    - .planning/phases/02-filtering-drilldowns-signal-detail/02-UI-SPEC.md (interaction contract, copywriting contract)
  </read_first>
  <action>
Run the dev server and perform the 20-step verification checklist below. Pause for the user to walk through it. Capture any failures verbatim and report them as a structured list (check number, observed behavior, expected behavior). If failures exist, the user should run `/gsd-plan-phase 02 --gaps` to schedule fixes; do NOT auto-fix from this plan.

Command to launch the dev server (run in project root):
```
npm run dev
```
Then guide the user through the 20 checks in the `<how-to-verify>` block.
  </action>
  <what-built>
    - Time period filter pills (7d/30d/90d/All) in the header bar, same row as the Churn / Enrollment & Upsell tabs
    - Per-tab independent filter state (each tab remembers its own filter)
    - All stat cards and charts on the active tab update when the filter changes
    - "Posts Ingested" stat card filters by captured_date when a non-All filter is active
    - CommunityChart, SignalVolumeChart, and SeverityChart are clickable (cursor: pointer); MatchRateChart, ConfidenceHistogram, PostsVsSignalsChart are NOT clickable
    - Clicking a chart bar slides in a 480px right-side drawer with a list of signal cards filtered to that bar's value
    - Clicking a signal card replaces the list with a detail view containing metadata + Key Quote + Summary + Suggested Action
    - "Back to signals" button returns to the list
    - Clicking the dimmed backdrop closes the drawer; reopening always shows the list (not the last detail)
    - Top Orgs table is removed from BOTH tabs
    - E&U tab now shows: "Enrollment Signals" + "Upsell Signals" stat cards, "Enrollment vs Upsell Volume Over Time" chart, "Signal Sources by Type" chart
  </what-built>
  <how-to-verify>
    Start the dev server in the project root:
    ```
    npm run dev
    ```
    Open the printed URL (typically http://localhost:5173) and verify each of the following:

    **Filter pills (D-01, D-02, D-03):**
    1. Confirm filter pills (7d / 30d / 90d / All) appear in the header bar to the right of the tab buttons, on the SAME ROW. They should not be on a separate row below the tabs.
    2. With Churn tab active, click "7d". All five stat cards (Total Signals, Match Rate, Unique Orgs, High Severity, Posts Ingested) AND all visible charts should update to show only the last 7 days.
    3. Confirm "Posts Ingested" shows a non-zero number (assuming any posts in last 7 days). If it shows 0, that's the captured_date Pitfall 1 — record the bug.
    4. Switch to Enrollment & Upsell tab. The filter should reset to its own state (default = All). Click "30d". The E&U tab should show last-30-days data.
    5. Switch back to Churn tab. The Churn filter should still be "7d" (per D-03 — per-tab state).

    **Drill-downs (D-04, D-05, D-06, D-07):**
    6. On the Churn tab, hover any bar in the Signals by Community chart — cursor should become a pointer. Click any bar (e.g. Reddit). A drawer should slide in from the right (~480px wide) with the title like "Reddit · Churn Signals".
    7. Confirm the drawer shows signal cards each with: org name, source, severity, confidence, match method, created date.
    8. Click the dimmed backdrop (anywhere outside the drawer panel). The drawer should close smoothly.
    9. Confirm that with the drawer closed, the page is fully interactive — try clicking a tab button or another bar. (Tests Pitfall 3.)
    10. Repeat for SignalVolumeChart (click a week stack — title should read "Week of YYYY-Wnn · ... Signals" with the matching signals listed). Repeat for SeverityChart (click a severity bar).
    11. Confirm MatchRateChart, ConfidenceHistogram, and PostsVsSignalsChart are NOT clickable (no pointer cursor, no drawer opens on click).

    **Detail view (D-08, D-09, D-10, D-11):**
    12. Open a drawer (any chart). Click a signal card. The list should be replaced by a detail view inside the SAME drawer.
    13. Confirm the detail view shows: a "Back to signals" button at the top, a metadata block (Org, Source, Signal Type, Severity, Confidence, Match Method, Created), then a "Key Quote" section with a blue-left-bordered quote block, a "Summary" paragraph, and a "Suggested Action" callout in a light green card.
    14. Click "Back to signals". The list should reappear.
    15. Click backdrop to close the drawer. Reopen the drawer (click any bar). Confirm it shows the list, NOT the last viewed detail (tests Pitfall 2).

    **E&U tab enrichment (D-12, D-13):**
    16. On the Enrollment & Upsell tab, confirm two new stat cards: "Enrollment Signals" and "Upsell Signals", each showing a distinct count.
    17. Confirm the "Enrollment vs Upsell Volume Over Time" stacked bar chart appears (blue + purple bars by week).
    18. Confirm the "Signal Sources by Type" horizontal bar chart appears, with TWO bars per source (one blue, one purple).

    **Top Orgs removal (D-14):**
    19. Confirm NO "Top Orgs" table exists on either tab.

    **Build / no console errors:**
    20. Open browser DevTools console. Refresh the page. Confirm no red errors. Warnings about React keys or similar are acceptable but note any.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
    Manual: All 20 checklist steps pass per user inspection of the dev server.
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (sanity check)
    - User explicitly types "approved" after walking through all 20 checks
    - OR user lists numbered failures, which are then routed to `/gsd-plan-phase 02 --gaps`
  </acceptance_criteria>
  <done>
    User has typed "approved" or has provided a written list of failed checks. Either way, this plan terminates and produces SUMMARY.md recording the outcome.
  </done>
  <resume-signal>Type "approved" if all 20 checks pass. Otherwise list the specific check numbers that failed and the observed behavior — failures will be addressed in a `/gsd-plan-phase 02 --gaps` follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries — this plan runs the dev server locally and inspects the UI. No data is sent or stored.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | I (Local secret leak via dev server) | Vite dev server reading .env.local | accept | Existing Phase 01 setup; .env.local not committed; anon key only |
</threat_model>

<verification>
Human approval after running 20 checklist items against the live dev server.
</verification>

<success_criteria>
- All 20 visual / interaction checks pass
- No browser console errors
- User explicitly types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/02-filtering-drilldowns-signal-detail/02-05-SUMMARY.md` capturing the verification result and any gaps found.
</output>
